# Netlify redirects. Format: from  to  status
# (force "!" so these win even though the old .html files still exist)

# Duplicate DUI page consolidated into /practice-areas/dui
/dui-defense           /practice-areas/dui   301!
/dui-defense.html      /practice-areas/dui   301!

# Employment law discontinued — send to homepage (was previously pointing to immigration)
/practice-areas/employment-law        /   301!
/practice-areas/employment-law.html   /   301!

# Personal injury discontinued — consolidated into immigration (matches the old stub's client-side redirect)
/practice-areas/personal-injury        /practice-areas/immigration   301!
/practice-areas/personal-injury.html   /practice-areas/immigration   301!

# Search Console ownership verification. Forced 200 so the exact .html path
# is served as-is and is not rewritten to an extensionless URL, which would
# break Google's check.
/google56619ba9581d555d.html   /google56619ba9581d555d.html   200!
